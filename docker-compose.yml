version: '3'

services:
  frontend:
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/
      - /app/node_modules
    ports:
      - 3000:3000
    environment: 
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
    command: ["yarn", "start"]